# bek_3_kyrs

Простое веб‑приложение / сервис обмена — BEK, 3 курс.  

## 📄 Описание  
Проект реализует backend + frontend для обменов предметами (items/trades).  
Позволяет:  
- получать список предметов,  
- создавать обмены между пользователями,  
- получать список обменов.  
Цель — учебный проект, демонстрирующий работу с PHP / JS (или стек, который используется), маршрутизацию, REST‑подобные запросы, и базовую логику обменов.  

## 📦 Что внутри / Структура проекта  
- `src/` — исходный код приложения, frontend + backend (или как организовано).  
- `db/` — файлы/скрипты для работы с базой данных.  
- `Dockerfile`, `docker-compose.yml` — контейнеризация (если используется Docker).  
- Другие вспомогательные файлы.  

## 🚀 Установка и запуск  

### Предварительные требования  
- PHP (если backend на PHP) / Node.js / другой стек — в зависимости от реализации.  
- (Если есть) Docker + docker-compose.  

### Запуск вручную  
1. Клонировать репозиторий:  
   ```bash
   git clone https://github.com/DaNuke-afk/bek_3_kyrs.git
   cd bek_3_kyrs
